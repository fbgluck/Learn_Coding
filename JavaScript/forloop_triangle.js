// Teaching Program - Output a Triangle
let bigstring = "#";
for (let counter = 1 ; counter<=7; counter++) {  // start at 0, execute while counter <=7, increment at end of loop
    console.log (bigstring + ' '+ counter);
    bigstring = bigstring + "#";
}
console.log ("done...");
